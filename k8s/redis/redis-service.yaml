apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: suntransit
spec:
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
    - name: redisinsight
      port: 8001
      targetPort: 8001
