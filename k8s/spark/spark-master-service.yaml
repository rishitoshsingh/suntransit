apiVersion: v1
kind: Service
metadata:
  name: spark-master
  namespace: suntransit
spec:
  selector:
    app: spark-master
  ports:
    - name: cluster
      port: 7077
      targetPort: 7077
    - name: webui
      port: 8080
      targetPort: 8080
      nodePort: 30808
  type: NodePort
